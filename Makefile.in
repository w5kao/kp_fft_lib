
CFLAGS=-march=native -O2 -g -flto -std=gnu99 -W -pedantic -Wcast-qual -Wpointer-arith -Wcast-align \
	-mavx2 -mfma -ffast-math @FFTW_CFLAGS@

LDFLAGS=@LDFLAGS@ @FFTW_LIBS@ -march=native -O2 -flto -lm -lpthread

cCC=@CC@


all: kpfft_test

%.o: %.c
	$(cCC) -c $(CFLAGS) -o $@ $<

kpfft_test: kpfft_test.o kpfft_lib.o my_thr_lib.o aff.o
	$(cCC) -Wall -o $@ $^ $(LDFLAGS)


.PHONY: clean distclean

clean:
	rm -f *.o

distclean: clean 
	rm kpfft_test



